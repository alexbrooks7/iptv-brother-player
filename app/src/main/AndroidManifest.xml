<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Required at runtime on API 33+ for the playback notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required by the Pawns bandwidth-sharing peer service on API 34+. The
         service only ever runs after the viewer explicitly accepts the consent
         dialog, and only in builds configured with a pawns.apiKey. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- A TV has no touchscreen and (usually) no camera/telephony. Declaring
         these as not-required means nothing here can cause a device to reject
         the app at install time. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <!-- Declares 10-foot/D-pad UI support. Not required=true: that attribute
         is purely a Play Store submission filter that restricts *store*
         distribution to devices advertising this feature — it is not enforced
         by the OS at install time, so it has no effect on sideloading, and
         this app is sideload-only. There used to be a separate manifest per
         store flavour over this one flag; both said the same thing in
         practice, so the flavour split was retired. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:name=".IptvApp"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IptvPlayer"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout|smallestScreenSize|uiMode">

            <!-- LEANBACK_LAUNCHER is what puts the app on the Android TV /
                 Fire TV home row. The plain LAUNCHER filter is kept so the app
                 is also launchable on generic boxes that ship a phone-style
                 launcher, which is common in the cheap-Android-TV-box segment. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".player.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
        <!-- Pawns bandwidth-sharing peer service.
             Foreground rather than background, deliberately: while this runs the
             device is acting as a gateway for someone else's internet traffic,
             and a foreground service is what guarantees a persistent, visible
             notification for as long as that is true. Nobody should have to
             wonder whether sharing is still on. Started only from
             PawnsManager.startSharing(), which is reached from the consent
             dialog or the Settings toggle. -->
        <service
            android:name="com.pawns.sdk.internal.service.PeerServiceForeground"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Allows to share internet traffic by modifying device's network settings to be used as a gateway for internet traffic. Device becomes a gateway which allows to send and receive internet traffic." />
        </service>
        <meta-data
            android:name="com.pawns.sdk.pawns_service_channel_name"
            android:value="Internet Sharing" />
    </application>
</manifest>
