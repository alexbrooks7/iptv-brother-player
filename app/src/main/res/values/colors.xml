<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the handful of colours the *platform* needs before Compose is running
  (window background, so a cold start does not flash white). Everything else
  lives in ui/theme/Color.kt.
-->
<resources>
    <!-- Must match ui/theme/Color.kt's NightBackground. -->
    <color name="window_background">#0B160F</color>
</resources>
